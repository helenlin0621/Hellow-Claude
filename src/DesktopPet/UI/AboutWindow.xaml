<Window x:Class="DesktopPet.UI.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="關於"
        Width="320" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <!--
        D5 關於視窗（設計檔 §6.3）：純靜態資訊展示，不依賴任何尚未實作的元件
        （存讀整合／多寵物皆屬 E1–E4），故可獨立完成。版本號取自組件版本
        （AboutWindow.xaml.cs 的 OnLoaded），不硬編字串，跟著發佈版本走。
    -->
    <StackPanel Margin="20">
        <TextBlock Text="DesktopPet" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" />
        <TextBlock x:Name="VersionText" Margin="0,4,0,12" HorizontalAlignment="Center" Foreground="Gray" />
        <TextBlock Text="在桌面上飼養 1–2 隻虛擬寵物的 Windows 應用。"
                   TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center" />
        <Button Content="確定" Width="80" Margin="0,16,0,0" HorizontalAlignment="Center"
                IsDefault="True" IsCancel="True" Click="OnCloseClick" />
    </StackPanel>
</Window>
